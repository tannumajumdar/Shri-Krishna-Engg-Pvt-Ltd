.env
node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*.local
next-env.d.ts
.vercel
*.tsbuildinfo

/lib/generated/prisma

# Uploaded media (served from /public/uploads); keep the folder, ignore files
/public/uploads/*
!/public/uploads/.gitkeep

# Deploy bundle (built artifact, never commit)
ske-cpanel-deploy.zip
